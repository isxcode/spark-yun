package com.isxcode.spark.api.api.constants;

/** 各个默认指定路径. */
public interface PathConstants {

    /** spark插件的安装路径. */
    String SPARK_MIN_HOME = "spark-min";

    /** flink插件的安装路径. */
    String FLINK_MIN_HOME = "flink-min";

    /** 插件文件夹. */
    String PLUGINS_DIR = "plugins";

    /** 依赖文件夹. */
    String LIB_DIR = "lib";

    /** 代理文件命令. */
    String AGENT_PATH_NAME = "zhiqingyun-agent";
}
