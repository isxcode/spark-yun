// 后端打包
tasks.register('package', GradleBuild) {

  // 前端打完包，后端才可以打包
  mustRunAfter(":spark-yun-frontend:package")

  // 后端代码检测
  dependsOn(":spotlessApply", ":checkstyleMain")

  // 构建后端
  tasks = [":spark-yun-backend:spark-yun-main:bootJar"]
}